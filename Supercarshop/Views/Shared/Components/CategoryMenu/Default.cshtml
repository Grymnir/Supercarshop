@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="Car"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Cars<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Car" asp-action="List"
                   asp-route-category="@category.CategoryId">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Car" asp-action="List" asp-route-Category="">View all cars</a>
        </li>
    </ul>
</li>